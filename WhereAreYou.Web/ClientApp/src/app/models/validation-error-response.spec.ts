import { ValidationErrorResponse } from './validation-error-response';

describe('ValidationErrorResponse', () => {
  it('should create an instance', () => {
    expect(new ValidationErrorResponse()).toBeTruthy();
  });
});
