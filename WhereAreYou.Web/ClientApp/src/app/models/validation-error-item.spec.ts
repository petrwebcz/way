import { ValidationErrorItem } from './validation-error-item';

describe('ValidationErrorItem', () => {
  it('should create an instance', () => {
    expect(new ValidationErrorItem()).toBeTruthy();
  });
});
